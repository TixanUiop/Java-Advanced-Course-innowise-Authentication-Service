spring:
  profiles:
    active: local
  application:
    name: authentication-service
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
admin:
  login: admin
  password: admin123
jwt:
  secret: ${JWT_SECRET:com-innowise-authentication-service-secret-key}